

/* === Language Switch (restored) === */
.lang-switch{display:flex;gap:.4rem;align-items:center}
.lang-switch button{padding:.35rem .7rem;border-radius:999px;border:1px solid rgba(255,255,255,.35);color:#fff;background:transparent;font-size:.85rem}
.lang-switch button.active{background:var(--gold);color:var(--navy);border-color:var(--gold)}

/* Brand stats under duo cards (optional, uses existing nodes if present) */
.brand-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:.6rem;border-top:1px solid rgba(0,0,0,.06);padding:.6rem 1rem 1rem}
.brand-stat{background:linear-gradient(180deg,rgba(212,175,55,.08),rgba(212,175,55,.02));border:1px solid rgba(212,175,55,.25);border-radius:12px;padding:.5rem .6rem;text-align:center}
.brand-stat .n{font-weight:800;color:var(--navy)}
.brand-stat .l{font-size:.78rem;color:var(--text-light)}

/* Gallery close primary button */
.gallery-close-primary{margin-left:auto;margin-right:auto;margin-top:.4rem;padding:.6rem 1rem;border-radius:10px;background:var(--gold);color:var(--navy);border:2px solid var(--gold);font-weight:700}
